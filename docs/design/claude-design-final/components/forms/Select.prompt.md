Native-backed select, styled to match Input height/radius. Used for category, workspace, role, and provider pickers.

```jsx
<Select label="الفئة" options={[{value:'groceries',label:'بقالة'},{value:'transport',label:'مواصلات'}]} />
```
