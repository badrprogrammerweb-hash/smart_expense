Used for empty workspaces and empty lists — pairs guidance with one clear primary action (never a mandatory wizard).

```jsx
<EmptyState icon="receipt" title="لا توجد مصاريف بعد" description="أضف أول مصروف لبدء تتبع ميزانيتك." actionLabel="إضافة مصروف" onAction={add} />
```
