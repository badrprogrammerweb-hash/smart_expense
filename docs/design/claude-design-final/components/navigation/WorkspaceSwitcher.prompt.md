Workspace picker for the top header — deliberately separate from the account/user menu (per brief §19, never merge the two).

```jsx
<WorkspaceSwitcher current={{name:'عائلة العتيبي',type:'عائلي'}} workspaces={[{name:'مشروعي الشخصي'}]} onSelect={set} />
```
