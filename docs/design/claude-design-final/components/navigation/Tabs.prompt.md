Underline tab bar — reports time-period switches, settings sub-sections.

```jsx
<Tabs tabs={[{key:'month',label:'هذا الشهر'},{key:'quarter',label:'الربع'},{key:'year',label:'السنة'}]} defaultValue="month" />
```
