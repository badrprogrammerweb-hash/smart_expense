App shell header: workspace switcher on one side, account menu on the other — never combined into one control.

```jsx
<TopHeader user={{name:'سارة العتيبي',role:'مالك'}} workspace={{name:'عائلة العتيبي',type:'عائلي'}} />
```
