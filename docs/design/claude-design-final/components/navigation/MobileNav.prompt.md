Fixed bottom tab bar for mobile — the compact-navigation pattern replacing the sidebar per the brief. "المزيد" opens a drawer with the remaining sidebar items (categories, files, AI review, history, settings).

```jsx
<MobileNav active="dashboard" onNavigate={go} />
```
