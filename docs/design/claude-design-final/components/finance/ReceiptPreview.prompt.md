Shows the original uploaded receipt/invoice beside the AI-extracted values — always true-to-source, `fileName` isolated LTR.

```jsx
<ReceiptPreview src={url} fileName="receipt-2026-07-13.jpg" onZoom={openLightbox} />
```
