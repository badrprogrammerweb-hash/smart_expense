General-purpose card for dashboard sections (recent activity, category breakdown, pending reviews). Set `padded={false}` when the body is a Table that needs edge-to-edge width.

```jsx
<InfoCard title="أحدث العمليات" actions={<Button variant="ghost" size="sm">عرض الكل</Button>}>…</InfoCard>
```
