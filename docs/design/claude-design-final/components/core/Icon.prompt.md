Thin wrapper that renders a Lucide icon by name (requires the Lucide CDN script loaded globally); falls back to a neutral square if the icon isn't found.

```jsx
<Icon name="wallet" size={20} />
```

Used inside Button, IconButton, Badge, Alert, SummaryCard, and Sidebar for all iconography — never hand-drawn SVG.
