Full-pill status chip. Color always pairs with a text label — never color-only.

```jsx
<Badge tone="warning" dot>قيد المراجعة</Badge>
<Badge tone="income">دخل</Badge>
```
Used for AI extraction states (not started/processing/ready/confirmed/failed/discarded), record status, and role labels (Owner/Admin/Member/Viewer).
