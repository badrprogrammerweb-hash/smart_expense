Floating confirmation for a just-completed action (record saved, receipt uploaded). Stack top-inline-end of the viewport.

```jsx
<Toast tone="success" title="تم حفظ المصروف" description="430.00 ر.س — بقالة" />
```
Use Alert instead when the message must persist on the page (e.g. pending API key).
