# Shared package

This directory owns cross-cutting constants, conventions, and documentation
shared across application boundaries.

For this foundation phase, the package is documentation-only. Cross-language
generated contracts or shared runtime code will be introduced only when a
later feature requires them.
